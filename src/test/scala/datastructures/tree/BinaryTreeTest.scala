package datastructures.tree
import org.scalatest.FunSpec

class BinaryTreeTest extends FunSpec {

  describe("A BinaryTree") {

    it("Should have no children if it has only one value") {
      assert(false)
    }
  }
}
